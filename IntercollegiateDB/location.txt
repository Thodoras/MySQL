CREATE TABLE LOCATION
(	LOCNO VARCHAR(4) NOT NULL,
	FACNO VARCHAR(4) NOT NULL,
	LOCNAME VARCHAR(21) NOT NULL,
	CONSTRAINT PK_LOCNO PRIMARY KEY (LOCNO),
	CONSTRAINT FK_FACNO_LOC FOREIGN KEY (FACNO) REFERENCES FACILITY (FACNO)
)